# Planned C++ component

No C++ implementation is included yet. Select a computational kernel after profiling the validated Python reference. Establish matching inputs, numerical accuracy, compiler settings and timing boundaries before reporting a speed comparison.
